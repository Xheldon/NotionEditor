import { InputRule, inputRules } from 'prosemirror-inputrules';
import { EditorState, Transaction } from 'prosemirror-state';

/*export default () => new InputRule(/\*!/, (state: EditorState, match: [string], start: number, end: number): Transaction => {
    return state.tr;
});*/
